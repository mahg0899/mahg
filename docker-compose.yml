version: '3'

services:
  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: mahg
      POSTGRES_PASSWORD: BFucm3WZG2haaR5HQ
      POSTGRES_DB: bentomahg
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
